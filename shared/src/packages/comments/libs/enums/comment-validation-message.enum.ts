const CommentValidationMessage = {
  COMMENT_TEXT_REQUIRE: 'Comment text is required',
  COMMENT_PUBLISHED_ISO_DATE:
    'The date of publication should be in ISO 8601 format',
} as const;

export { CommentValidationMessage };
